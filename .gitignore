# secrets
.env
config.yaml

# local state (sqlite db, cron logs)
state/
*.db

# ローカルでカスタマイズするscraper方式の実装（各自の環境固有のため非公開、テンプレートのみ公開）
scrapers/*
!scrapers/example-blog/

# python
__pycache__/
*.pyc
*.pyo
.venv/
.python-version

# editor
.DS_Store

# MCP server config (local only)
.mcp.json
.claude/settings.local.json

# 組織内エージェントの作業メモリ（内部情報を含むため非公開）
.claude/agent-memory/
